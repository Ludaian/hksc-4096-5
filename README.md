# HKSC-4096 – HyperKnight Supercube Cryptosystem

**Crypto primitive mạnh nhất 2026**: 16×16×16 supercube + 3D knight tour 4096 cells + self-solving + √2 fractional + adversarial + zk-SNARK + 100-hour VDF time-lock.

## Features
- On-chain verifier: `hksc-verifier.eth`
- Electron GUI + 3D realtime animation
- Rubik Onion 100 layers (100 giờ lock)
- In 3D model .obj

## Quick Start
```bash
# Python backend
cd hksc-python && pip install -r requirements.txt
python hksc_4096.py

# Electron GUI
cd hksc-electron && npm install && npm start
